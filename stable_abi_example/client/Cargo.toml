[package]
name = "client"
version = "0.1.0"
authors = ["Jozef Podlecki"]
edition = "2024"
rust-version = "1.85"

[dependencies]
tokio = { version = "1.41.0", features = ["rt", "rt-multi-thread", "signal", "time", "sync", "macros"] }
anyhow = "1.0.96"
abi_stable = "0.11.3"
shared = { path = "../shared" }