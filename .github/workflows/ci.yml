name: CI

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.89.0
      - run: Get-Location
      - run: ./replace-Toml.ps1
        shell: pwsh
        working-directory: ./buildtest

      - run: cargo build --release
        working-directory: ./buildtest
