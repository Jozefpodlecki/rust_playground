
[package]
name = "utils"
version = "0.1.0"
edition = "2024"

[dependencies]
winapi = { version = "0.3.9", features = ["errhandlingapi", "minwinbase", "libloaderapi"] }
ntapi = { version = "0.4.3"}

[features]
default = []
alloc = []
