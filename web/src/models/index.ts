
export interface LoadResult {
    appName: string,
    rustVersion: string,
    githubLink: string,
    version: string,
	loadedOn: string;
}

export interface Exercise {
    id: number;
    name: string;
}