import { useEffect, useState } from "react";
import Markdown from 'react-markdown';
// import remarkGfm from 'remark-gfm';

const Example: React.FC = () => {

	useEffect(() => {
	
	}, []);

	return (
		<section className="flex h-full w-full p-4 gap-4">
		
		</section>
	);
};

export default Example;
