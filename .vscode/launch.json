{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/app/target/debug/app.exe",
            "cwd": "${workspaceRoot}/app",
        }
    ]
}