{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo build (app)",
            "type": "shell",
            "command": "cargo build",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/app"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$rustc"]
        }
    ]
}